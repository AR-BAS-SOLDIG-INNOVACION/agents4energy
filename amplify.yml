version: 1
backend:
  phases:
    preBuild:
      commands:
        - echo "Build environment is $CODEBUILD_BUILD_IMAGE"
        # - aws ecr-public get-login-password | docker login --username AWS --password-stdin public.ecr.aws
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - npx ampx pipeline-deploy --branch "${AWS_BRANCH:?AWS_BRANCH is required}" --app-id "${AWS_APP_ID:?AWS_APP_ID is required}" --debug
frontend:
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
      - node_modules/**/*